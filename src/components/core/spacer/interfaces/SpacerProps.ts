import { type ArborTransformProps } from '../../../../ecosystem';

/**
 * @platform shared
 * Espaçador flexível que funciona em web e React Native via ArborTransform.
 */
export type SpacerProps = ArborTransformProps;
