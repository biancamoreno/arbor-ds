import { opacity as primitiveOpacity } from '../primitives';

export const opacity = {
  none: primitiveOpacity.layer[0],
  low: primitiveOpacity.layer[24],
  medium: primitiveOpacity.layer[48],
  high: primitiveOpacity.layer[72],
};
