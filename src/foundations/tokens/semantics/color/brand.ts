export type BrandPalette = {
  primary: string;
  secondary: string;
  accent: string;
  onPrimary: string;
  onSecondary: string;
};

export type BrandShades = {
  subtle: string;
  soft: string;
  base: string;
  strong: string;
  hover: string;
  active: string;
};
