// Mocks compartilhados pelo project Jest "native".
// Centralize aqui qualquer mock de módulo RN-only para evitar duplicação por teste.
//
// Quando entrar uma dependência native (ex.: react-native-reanimated, react-native-gesture-handler),
// adicione o jest.mock(...) correspondente neste arquivo e mantenha
// transformIgnorePatterns em jest.config.native.cjs alinhado.
